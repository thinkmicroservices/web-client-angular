import { TitleizerDirective } from './titleizer.directive';

describe('TitleizerDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleizerDirective();
    expect(directive).toBeTruthy();
  });
});
